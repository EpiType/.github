name: CI

on:
  push:
    branches: [$default-branch]
  pull_request:
    branches: [$default-branch]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      
      - name: Setup
        run: echo "Configurer l'environnement ici"
        
      - name: Build
        run: echo "Exécuter le build ici"
        
      - name: Test
        run: echo "Exécuter les tests ici"
